## Packages
framer-motion | Fluid page transitions, enter animations, and complex layout changes
date-fns | Formatting timestamps and dates beautifully
clsx | Conditional class names combining
tailwind-merge | Merging tailwind classes effectively

## Notes
- App uses Replit Auth with `/api/login` and `/api/logout` endpoints.
- Dark theme glassmorphism aesthetic is explicitly requested and set as default.
- We assume all API responses match the `@shared/schema` types defined in the prompt.
